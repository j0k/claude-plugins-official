{
  "name": "claude-channel-telegram",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "daemon": "bun install --no-summary && bun daemon.ts",
    "daemon:dev": "bun --watch daemon.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "grammy": "^1.21.0"
  }
}
